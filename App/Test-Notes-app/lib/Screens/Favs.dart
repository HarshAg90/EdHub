import 'package:flutter/material.dart';
import '../utils/routes.dart';

class favs extends StatelessWidget {
  const favs({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Favourites will come here")),
    );
  }
}
