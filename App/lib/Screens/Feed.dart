import 'package:flutter/material.dart';
import '../utils/routes.dart';

class feed extends StatelessWidget {
  const feed({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Feed will come here")),
    );
  }
}
